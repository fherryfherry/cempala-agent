## Description

<!-- What does this PR do, and why? Reference the ticket: MAP-NNN -->

## Checklist

- [ ] `make test` passes (backend pytest)
- [ ] `npm run lint` passes (frontend)
- [ ] Commit messages reference the ticket (`MAP-NNN`)
- [ ] Docs updated in the same change if the change affects documented behavior
- [ ] No destructive commands / secrets / DB files added

## Testing done

<!-- How did you verify this? For agent-adapter changes: was it tested against a fake
binary, not real LLM calls? -->

## Screenshots (if UI change)
